# 生产环境配置示例
# 复制此文件为 .env.prod 并修改配置

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
GIN_MODE=release

# 数据库配置
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here
DB_DATABASE=verilog_oj
DB_SSL_MODE=disable

# Redis 配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here
REDIS_DB=0

# JWT 配置
JWT_SECRET=your_very_long_and_random_jwt_secret_key_here
JWT_EXPIRES_IN=24

# 消息队列配置
QUEUE_TYPE=redis
QUEUE_HOST=redis
QUEUE_PORT=6379
QUEUE_PASSWORD=your_redis_password_here
QUEUE_NAME=judge_queue

# 初始管理员配置（仅首次部署时创建）
# ⚠️ 重要：首次部署后请删除这些环境变量或设置为空
# 如果数据库中已有管理员用户，这些配置会被忽略
INIT_ADMIN_USERNAME=your_admin_username
INIT_ADMIN_EMAIL=admin@yourdomain.com
INIT_ADMIN_PASSWORD=your_very_secure_password_here
