openapi: 3.0.3
info:
  title: Verilog OJ News API
  description: Verilog在线判题系统新闻管理API
  version: 1.0.0
  contact:
    name: API Support
    email: support@verilog-oj.com
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: http://localhost:8080/api/v1
    description: 开发环境
  - url: https://api.verilog-oj.com/api/v1
    description: 生产环境

# 引用组件定义
components:
  $ref: './models/common.yaml#/components'

paths:
  /news:
    get:
      tags:
        - 新闻管理
      summary: 获取新闻列表
      security:
        - BearerAuth: []
      x-rbac-permissions: [news.list]
      description: 需要新闻列表查看权限
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: category
          in: query
          schema:
            type: string
      responses:
        '200':
          description: 获取成功
          content:
            application/json:
              schema:
                $ref: './models/news.yaml#/components/schemas/NewsListResponse'
        '403':
          description: 权限不足
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'

    post:
      tags:
        - 新闻管理
      summary: 创建新闻
      security:
        - BearerAuth: []
      x-rbac-permissions: [news.create]
      description: 需要新闻创建权限
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './models/news.yaml#/components/schemas/CreateNewsRequest'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: './models/news.yaml#/components/schemas/NewsCreateResponse'
        '400':
          description: 请求参数错误
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'
        '403':
          description: 权限不足 - 需要 news.create 权限
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'

  /news/{id}:
    get:
      tags:
        - 新闻管理
      summary: 获取新闻详情
      security:
        - BearerAuth: []
      x-rbac-permissions: [news.read]
      description: 需要新闻读取权限
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: 获取成功
          content:
            application/json:
              schema:
                $ref: './models/news.yaml#/components/schemas/NewsDetailsResponse'
        '403':
          description: 权限不足 - 需要 news.read 权限
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'
        '404':
          description: 新闻不存在
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'

    put:
      tags:
        - 新闻管理
      summary: 更新新闻
      security:
        - BearerAuth: []
      x-rbac-permissions: [news.update]
      description: 需要新闻更新权限
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './models/news.yaml#/components/schemas/UpdateNewsRequest'
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                $ref: './models/news.yaml#/components/schemas/NewsUpdateResponse'
        '400':
          description: 请求参数错误
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'
        '403':
          description: 权限不足 - 需要 news.update 权限
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'

    delete:
      tags:
        - 新闻管理
      summary: 删除新闻
      security:
        - BearerAuth: []
      x-rbac-permissions: [news.delete]
      description: 需要新闻删除权限
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                $ref: './models/news.yaml#/components/schemas/NewsDeleteResponse'
        '403':
          description: 权限不足 - 需要 news.delete 权限
          content:
            application/json:
              schema:
                $ref: './models/common.yaml#/components/schemas/Error'